info:
  name: Stream Subagent Status
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/api/v1/chat/stream/:thread_id/status
  headers:
    - name: X-User-Id
      value: test-user-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
