info:
  name: Soft Interrupt Workflow
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/api/v1/workflow/:thread_id/soft-interrupt
  headers:
    - name: X-User-Id
      value: test-user-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
