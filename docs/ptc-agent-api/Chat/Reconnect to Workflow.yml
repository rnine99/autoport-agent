info:
  name: Reconnect to Workflow
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/chat/stream/:thread_id/reconnect
  params:
    - name: last_event_id
      value: "42"
      enabled: false
  headers:
    - name: X-User-Id
      value: test-user-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
