info:
  name: Cancel Workflow
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8000/api/v1/chat/cancel/:thread_id
  headers:
    - name: X-User-Id
      value: test-user-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
