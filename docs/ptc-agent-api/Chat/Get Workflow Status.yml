info:
  name: Get Workflow Status
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/api/v1/chat/status/:thread_id
  headers:
    - name: X-User-Id
      value: test-user-001
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
